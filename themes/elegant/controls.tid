title: $:/themes/tiddlywiki/elegant/controls
tags: [[$:/tags/Stylesheet]]

\rules only filteredtranscludeinline transcludeinline macrodef macrocallinline

/*
** Buttons, tags, controls, inputs, dropdowns & modals
**
** This is the chrome register: the quiet sans, square corners, hairline edges.
** Nothing here is frosted. Editorial pages are made of paper, not glass, and a
** blurred panel over a wiki is decoration pretending to be depth. The only
** surfaces that carry a shadow are the ones that genuinely leave the page —
** dropdowns, modals, notifications.
*/

@keyframes e-rise {
	from { opacity: 0; transform: translate3d(0, 6px, 0); }
	to   { opacity: 1; transform: translate3d(0, 0, 0); }
}

/* Toolbar icons: quiet by default, warming to the accent. One signal — the
   glyph changes colour. No scale: a toolbar that flinches when the pointer
   crosses it is noise, and eleven of them flinching is a nervous tic. */
.tc-tiddler-controls button,
.tc-page-controls button,
.tc-topbar button {
	border-radius: var(--e-radius);
	transition: background-color var(--e-dur) var(--e-ease);
}
.tc-tiddler-controls button svg,
.tc-page-controls button svg,
.tc-topbar button svg {
	transition: fill var(--e-dur) var(--e-ease);
}
.tc-tiddler-controls button:hover,
.tc-page-controls button:hover,
.tc-topbar button:hover {
	background: var(--e-tint);
}
.tc-tiddler-controls button:hover svg,
.tc-page-controls button:hover svg,
.tc-topbar button:hover svg {
	fill: var(--e-accent);
}

/* Standard buttons: a hairline box in the chrome sans, filling with nothing.
   The hover moves the edge and the label together, once. */
button.tc-btn-standard,
.tc-btn-boxed,
.tc-btn-rounded {
	font-family: var(--e-sans);
	border-radius: var(--e-radius);
	border: 1px solid var(--e-rule-strong);
	transition: border-color var(--e-dur) var(--e-ease),
		color var(--e-dur) var(--e-ease);
}
button.tc-btn-standard:hover,
.tc-btn-boxed:hover,
.tc-btn-rounded:hover {
	border-color: var(--e-accent);
	color: var(--e-accent-deep);
}

/* Primary / call-to-action: the one solid block of accent in the whole theme */
.tc-btn-big-green,
.tc-primary-btn,
button.tc-btn-big-green {
	font-family: var(--e-sans);
	background: <<colour primary>> !important;
	color: <<colour background>> !important;
	border: 1px solid <<colour primary>> !important;
	border-radius: var(--e-radius);
	box-shadow: none;
	transition: background-color var(--e-dur) var(--e-ease),
		border-color var(--e-dur) var(--e-ease);
}
.tc-btn-big-green svg,
.tc-primary-btn svg { fill: <<colour background>> !important; }
.tc-btn-big-green:hover,
.tc-primary-btn:hover {
	background: var(--e-accent-deep) !important;
	border-color: var(--e-accent-deep) !important;
}

/* Tags: flat editorial labels — a hairline box, no fill, no ring inside a ring.
   The doubled class lifts specificity above vanilla's `span.tc-tag-label`
   so the palette's loud chip colour is replaced regardless of load order. */
.tc-tag-label.tc-tag-label:not([style*="background-color"]),
.tc-tag-label.tc-tag-label[style*="background-color:;"],
.tc-tag-label.tc-tag-label[style*="background-color: ;"] {
	background-color: transparent;
	color: <<colour foreground>> !important;
	fill: <<colour foreground>> !important;
	box-shadow: inset 0 0 0 1px var(--e-rule-strong);
}
.tc-tag-label.tc-tag-label:not([style*="background-color"]):hover,
.tc-tag-label.tc-tag-label[style*="background-color:;"]:hover,
.tc-tag-label.tc-tag-label[style*="background-color: ;"]:hover {
	background-color: var(--e-tint);
	box-shadow: inset 0 0 0 1px var(--e-accent);
}
.tc-tag-label.tc-tag-label:not([style*="background-color"]) svg,
.tc-tag-label.tc-tag-label[style*="background-color:;"] svg,
.tc-tag-label.tc-tag-label[style*="background-color: ;"] svg { fill: <<colour foreground>> !important; }

/* Themed shape + a subtle defining edge on every tag. Coloured tags
   (#hex, rgb(), or a named colour like GreenYellow) keep their own fill
   and contrastcolour text and gain this edge; the uncoloured chip above
   overrides the box-shadow with its hairline. */
.tc-tag-label.tc-tag-label {
	font-family: var(--e-sans);
	font-size: 0.8em;
	letter-spacing: 0.03em;
	border-radius: var(--e-radius);
	box-shadow: inset 0 0 0 1px var(--e-rule-strong);
	transition: box-shadow var(--e-dur) var(--e-ease),
		background-color var(--e-dur) var(--e-ease);
}

.tc-untagged-label {
	border-radius: var(--e-radius);
}

/* Dropdowns: an opaque card of paper lifted off the page, hairline-edged */
.tc-drop-down,
.tc-block-dropdown {
	font-family: var(--e-sans);
	border-radius: var(--e-radius);
	border: 1px solid var(--e-rule-strong);
	background: <<colour dropdown-background>>;
	box-shadow: var(--e-lift);
	padding: var(--e-space-3xs);
	animation: e-rise var(--e-dur) var(--e-ease) both;
}
.tc-drop-down a.tc-tiddlylink,
.tc-drop-down button,
.tc-drop-down .tc-dropdown-item,
.tc-block-dropdown a.tc-tiddlylink {
	border-radius: var(--e-radius);
	transition: background-color var(--e-fast) var(--e-ease);
}
/* Hover rows: vanilla pairs a primary background with near-white text; since we
   replace the background with a light wash, we must restore a dark, contrasting
   foreground (and icon fill) so the text never sits white-on-pale. */
.tc-drop-down a:hover,
.tc-drop-down button:hover,
.tc-drop-down .tc-btn-invisible:hover,
.tc-drop-down .tc-file-input-wrapper:hover button,
.tc-block-dropdown a:hover,
.tc-block-tags-dropdown .tc-btn-invisible:hover {
	background: var(--e-wash-strong);
	color: var(--e-ink);
}
.tc-drop-down button.tc-btn-invisible:hover svg,
.tc-block-dropdown button.tc-btn-invisible:hover svg,
.tc-block-tags-dropdown .tc-btn-invisible:hover svg {
	fill: var(--e-ink);
}
.tc-drop-down .tc-tab-divider,
.tc-drop-down hr {
	border: none;
	height: 1px;
	background: var(--e-rule);
}

/* Modals: opaque, hairline-framed, genuinely raised */
.tc-modal {
	border-radius: var(--e-radius);
	border: 1px solid var(--e-rule-strong);
	background: <<colour modal-background>>;
	box-shadow: var(--e-lift-high);
	overflow: hidden;
	animation: e-rise var(--e-slow) var(--e-ease) both;
}
.tc-modal-header { border-bottom: 1px solid var(--e-rule); }
.tc-modal-footer {
	border-top: 1px solid var(--e-rule);
	background: var(--e-tint);
}

/* Notifications & alerts */
.tc-notification {
	border-radius: var(--e-radius);
	border: 1px solid var(--e-rule-strong);
	background: <<colour notification-background>>;
	box-shadow: var(--e-lift);
	animation: e-rise var(--e-dur) var(--e-ease) both;
}
.tc-alert {
	border-radius: var(--e-radius);
	box-shadow: var(--e-lift);
}

/* Tiddler info panel — inline content, not a floating surface, so it gets rules
   rather than a box and arrives without an animation. Full-bleed to the frame
   edges via the gutter variable, then re-padded so its text keeps the measure. */
.tc-tiddler-info {
	margin-inline: calc(var(--e-frame-pad-x, 0px) * -1);
	padding-inline: var(--e-frame-pad-x, 0px);
	border-radius: 0;
	border: none;
	border-top: 1px solid var(--e-rule);
	border-bottom: 1px solid var(--e-rule);
	box-shadow: none;
}

/*
** Inputs & editors: a quiet field that takes an accent edge on focus.
**
** The border-colour shift is decoration and may transition. The focus RING is
** the accessibility indicator and must not: it comes from the `:focus-visible`
** outline in variables.tid, which is never transitioned and so is on screen the
** instant the field is reached. These rules deliberately do NOT set
** `outline: none` — an attribute selector plus `:focus` outranks the
** zero-specificity `:where(...):focus-visible` base rule, so suppressing the
** outline here would leave keyboard users with a ring that fades in over
** 220ms, which is to say with no ring at the moment they need one.
*/
input[type="text"],
input[type="search"],
input.tc-edit-texteditor,
textarea.tc-edit-texteditor,
.tc-edit-texteditor,
select {
	border-radius: var(--e-radius);
	transition: border-color var(--e-dur) var(--e-ease);
}
input[type="text"]:focus,
input[type="search"]:focus,
input.tc-edit-texteditor:focus,
textarea.tc-edit-texteditor:focus,
.tc-edit-texteditor:focus {
	border-color: var(--e-accent);
}
.tc-edit-tags { border-radius: var(--e-radius); }

/* Disabled controls: three channels, never opacity alone */
button:disabled,
button[aria-disabled="true"],
input:disabled,
textarea:disabled,
select:disabled {
	opacity: 0.55;
	cursor: not-allowed;
	border-color: var(--e-rule);
}
button:disabled:hover,
button[aria-disabled="true"]:hover {
	background: none;
	border-color: var(--e-rule);
	color: inherit;
}

/* Tabs: a rule drawn under the chosen tab */
.tc-tab-buttons button {
	border-radius: var(--e-radius);
	font-family: var(--e-sans);
	transition: color var(--e-dur) var(--e-ease);
}
.tc-tab-buttons button:hover { color: var(--e-accent-deep); }
.tc-tab-buttons button.tc-tab-selected {
	box-shadow: inset 0 -2px 0 var(--e-accent);
	color: <<colour foreground>>;
}

/* Plugin chips */
.tc-plugin-info {
	border-radius: var(--e-radius);
	border-color: var(--e-rule-strong);
	box-shadow: none;
	transition: border-color var(--e-dur) var(--e-ease);
}
/* Override vanilla's flood-fill hover (0,3,1) with Elegant's hairline shift. */
a.tc-tiddlylink.tc-plugin-info:hover {
	background-color: <<colour tiddler-background>>;
	color: <<colour foreground>>;
	fill: var(--e-accent-quiet);
	border-color: var(--e-accent);
	box-shadow: none;
	text-decoration: none;
}
a.tc-tiddlylink.tc-plugin-info:hover > .tc-plugin-info-chunk > svg {
	fill: var(--e-accent);
}
a.tc-tiddlylink.tc-plugin-info:hover > .tc-plugin-info-chunk .tc-plugin-info-stability {
	color: <<colour foreground>>;
	border-color: <<colour tiddler-border>>;
}
